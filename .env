# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# NOTE: In production (AWS), use strong, unique passwords!
DB_USER=myuser
DB_PASSWORD=mypassword
DB_HOST=db
DB_PORT=5432
DB_NAME=businessform_db

# Security
# For debugging, set to a single allowed origin (adjust as needed for your frontend)
CORS_ORIGIN=http://localhost:8080
